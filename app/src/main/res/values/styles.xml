<?xml version="1.0" encoding="utf-8"?>
<resources>


    <!--自定义Dialog样式-->
    <style name="BaseDialogFragment" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>

        <!-- 背景透明 -->
        <item name="android:windowBackground">@color/color_black_50</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- Dialog  对话框高亮渐变按钮 -->

    <style name="dialog_button_style" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/bg_stroke_btn_radius_28</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
    <!-- Dialog  对话框默认线条按钮 -->
    <style name="dialog_button_stroke_style" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/bg_stroke_btn_default</item>
        <item name="android:textColor">#333333</item>
    </style>

    <style name="DialogTitleTextStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/color_black</item>
    </style>
</resources>